{% extends 'base.html.twig' %}

{% block body %}
    <h1>Locale</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ locale.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ locale.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ locale.slug }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('locale_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('locale_edit', { 'id': locale.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
