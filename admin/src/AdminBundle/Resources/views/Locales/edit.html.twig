{% extends 'AdminBundle::_shared.html.twig' %}
{% block title %}Editer Langue{% endblock %} 
{% block body %}
{{ form_start(edit_form) }}
<div class="col col_12">
    <div class="container_box" data-change="{{ path('slugValidation',{ 'entity': '999-entity','slug':'888-slug'}) }}">
        <div class="box_title"> 
            <h3>Editer {{ locale.name }}</h3>
            <div class="header_for_element_state">
                {# <div class="listGroup">
                    <div class="option {% if locale.state == -1 %}active{% endif %}" data-index="{% if locale.state == -1 %}2{% else %}1{% endif %}" data-value="-1">Brouillon</div>
                    <div class="option {% if locale.state == 0 %}active{% endif %}" data-index="{% if locale.state == 0 %}2{% else %}1{% endif %}" data-value="0">Off-Line</div>
                    <div class="option {% if locale.state == 1 %}active{% endif %}" data-index="{% if locale.state == 1 %}2{% else %}1{% endif %}" data-value="1">On-Line</div>
                </div> #}
            </div>
        </div>
        {{ form_widget(edit_form.state ) }}
        <div class="box_content">
            <div class="row">
                {{ render(controller( 
                    'AdminBundle:RenderTemplate:renderLocaleForm',{ 'form': edit_form }
                )) }}
            </div>
        </div>
    </div>
</div>
<div class="col col_10 marginTopMinus">
    <div class="submitResetModifBtnsSection">
         <input type="submit" value="Enregistrer"  class="btn btn-submit"/>
         <input type="reset" value="Annuler"  class="btn btn-reset"/>
    </div>
</div>
{{ form_end(edit_form) }}
{% endblock %}


{% block local_script %}
    <script src="{{ asset('adminTemplateFiles/build/js/boxes_script.js') }}" type="text/javascript"></script>
{% endblock %}

