{% extends 'AdminBundle::_shared.html.twig' %}
{% block lib_script %}
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js "></script>
{% endblock %}

{% block local_script %}
    <script src="{{ asset('adminTemplateFiles/build/js/eventOrder.js') }}" type="text/javascript"></script>
    <script type="text/javascript">
    updatePositionOnEdit();
    </script>
{% endblock %}

{% block body %}
<div class="Nb">
    <p>Le changement d'ordre se fait via un drag and drop <span>Ordre Effectué</span> </p>
</div>
<div class="row orderPubLictionperTheme">
    {% for client in clients if client.state == true %}
        <div class="col col_1 publicationsOrderElement parent-img-position-picto" 
        data-path="{{path('updateClientPosition', { 'id': client.id,'position':'element' })}}" 
        data-id="{{client.id}}" 
        data-order="{{client.position}}">
            <div class="content">
                <div class="picto">
                        {% if client.clientlogo.src is defined and  client.clientlogo.src is not null %}
                            <img class="img-position-picto" src="{{ asset('uploads/'~ client.clientlogo.src) }}">
                        {% else %}
                            <img class="img-position-picto" src="{{ asset('adminTemplateFiles/build/img/noimage.png') }}">
                        {% endif %}
                    </div>
                <div class="title"><h3>{{client.translations[app.request.locale].name}}</h3>   </div>
            </div>
        </div>
    {% endfor %}
</div>
{% endblock %}
